
    // operadores matematicos

    //soma
    var x = 20;
    var y = 22;
    var soma = x + y;
    console.log(soma);

    //cuidado c isso
    var text = "20";
    soma = soma + text; // 4220 xD
    console.log(soma);

    //divisao

    var sub = y - x;
    console.log(sub);

    //multiplicacao

    var mult = x * y;
    console.log(mult);
    
    //modulo 

    var mod = y % 2;
    console.log(mod);

    /* incremento e decremento
    
    depois da var (numero--/numero++) primeiro imprime e depois decrementa/incrementa
    antes da var (--numero/++numero) primeiro faz a operação depois imprime
    */

    